class StickerTheme{
  final String imgBatch;
  final String title;
  final List<String> codes;
  final String codePrefix;

  const StickerTheme(this.imgBatch, this.title, this.codes, this.codePrefix);
}